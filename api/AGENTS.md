@../AGENTS.md

<!--
The line above imports the team guide at the repository root. If your tool does
not resolve `@` imports, open ../AGENTS.md and read it before writing any code —
it carries the git workflow, module ownership, migration rules, and SRS rules
that apply to this project. There are no api-only rules; the root file is the
single source.
-->
